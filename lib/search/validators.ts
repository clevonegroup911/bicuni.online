import{z}from"zod";
export const searchQuerySchema=z.object({q:z.string().trim().max(300).default(""),page:z.coerce.number().int().min(1).default(1),pageSize:z.coerce.number().int().min(1).max(50).default(15),sort:z.enum(["relevance","recent","views","downloads","citations","alphabetical"]).default("relevance"),university:z.string().trim().max(120).optional(),faculty:z.string().trim().max(120).optional(),department:z.string().trim().max(120).optional(),author:z.string().trim().max(120).optional(),language:z.string().trim().max(12).optional(),country:z.string().trim().max(80).optional(),year:z.coerce.number().int().min(1800).max(new Date().getFullYear()+1).optional(),category:z.string().trim().max(120).optional(),type:z.string().trim().max(40).optional(),status:z.enum(["APPROVED","PUBLISHED"]).optional(),license:z.string().trim().max(120).optional()});
