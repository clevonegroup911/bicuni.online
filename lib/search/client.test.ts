import{describe,expect,it}from"vitest";import{directoryIndexSettings,documentIndexSettings}from"./client";
describe("configuration des index",()=>{it("déclare tous les attributs filtrables demandés",()=>expect(documentIndexSettings.filterableAttributes).toEqual(expect.arrayContaining(["universityName","facultyName","departmentName","authorName","language","country","year","category","type","status","license"])));it("désactive les fautes sur les années",()=>expect(documentIndexSettings.typoTolerance.disableOnNumbers).toBe(true));it("indexe l’annuaire académique séparément",()=>expect(directoryIndexSettings.filterableAttributes).toContain("kind"))});
