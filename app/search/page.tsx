import type{Metadata}from"next";import{SearchExperience}from"@/components/search/search-experience";import{Breadcrumb}from"@/components/ui/breadcrumb";
export const metadata:Metadata={title:"Recherche académique",description:"Rechercher dans les publications, chercheurs et institutions académiques indexés par BICUNI."};export const dynamic="force-dynamic";
export default async function SearchPage({searchParams}:{searchParams:Promise<Record<string,string|string[]|undefined>>}){const raw=await searchParams;const initial=Object.fromEntries(Object.entries(raw).map(([key,value])=>[key,Array.isArray(value)?value[0]:value]));return <main className="shell"><header className="page-hero search-page-hero"><Breadcrumb items={[{label:"Accueil",href:"/"},{label:"Recherche"}]}/><span className="eyebrow">Moteur académique BICUNI</span><h1>Trouver ce qui compte.</h1><p>Publications, chercheurs, auteurs, universités, facultés et départements réunis dans une recherche tolérante aux erreurs.</p></header><SearchExperience initial={initial}/></main>}
