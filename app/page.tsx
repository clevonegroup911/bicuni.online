import Link from"next/link";import{ArrowRight,BrainCircuit,Building2,FileSearch,GraduationCap,Quote,ShieldCheck}from"lucide-react";import{DocumentGrid}from"@/components/documents/document-grid";import{AuthorCard,EntityEmpty,UniversityCard}from"@/components/marketing/entity-cards";import{PricingCard}from"@/components/marketing/pricing-card";import{SearchHero}from"@/components/marketing/search-hero";import{StatisticsCards}from"@/components/marketing/statistics-cards";import{Reveal}from"@/components/ui/motion";import{DocumentService}from"@/lib/documents/document-service";import{PLAN_CATALOG}from"@/lib/subscriptions/catalog";
export const dynamic="force-dynamic";
const features=[{icon:FileSearch,title:"Recherche précise",text:"Découvrez les travaux par discipline, auteur, institution, année et métadonnées académiques."},{icon:ShieldCheck,title:"Préservation privée",text:"Fichiers privés, accès temporaires et validation institutionnelle protègent chaque dépôt."},{icon:BrainCircuit,title:"Intelligence documentaire",text:"Une architecture prête pour l’OCR, les résumés, la bibliographie et la recherche intelligente."},{icon:Building2,title:"Gouvernance universitaire",text:"Des espaces institutionnels pour valider, administrer et mesurer la production scientifique."},{icon:GraduationCap,title:"Identité académique",text:"Des publications reliées à leurs auteurs, établissements, facultés et départements."}];
export default async function Home(){const result=await new DocumentService().listPublic({page:1,pageSize:6});const universities=aggregate(result.items.map(item=>item.university?.name).filter((name):name is string=>Boolean(name)));const authors=aggregate(result.items.map(item=>item.author.name).filter((name):name is string=>Boolean(name)));return <main><SearchHero/><section className="stats-band"><div className="shell"><StatisticsCards documents={result.total} universities={universities.length} authors={authors.length}/></div></section><section className="section"><div className="shell"><Reveal><div className="section-head"><div><span className="eyebrow">Une infrastructure, pas un dépôt PDF</span><h2>De la recherche<br/>à l’impact.</h2></div><p>BICUNI organise le cycle académique complet : dépôt sécurisé, validation, publication, découverte et rayonnement institutionnel.</p></div></Reveal><div className="feature-bento">{features.map(({icon:Icon,title,text},index)=><Reveal key={title} delay={index*.05} className={index<2?"feature-wide":""}><article className="glass card feature-card card-hover"><div className="feature-icon"><Icon/></div><h3>{title}</h3><p>{text}</p></article></Reveal>)}</div></div></section><section className="section publications-section"><div className="shell"><div className="section-head"><div><span className="eyebrow">Publications validées</span><h2>Le savoir en mouvement.</h2></div><Link href="/documents" className="button secondary">Toutes les publications<ArrowRight size={16}/></Link></div><DocumentGrid documents={result.items}/></div></section><section className="section"><div className="shell"><div className="section-head"><div><span className="eyebrow">Réseau académique</span><h2>Des institutions reliées.</h2></div><p>Les établissements et auteurs présentés proviennent exclusivement des publications réellement validées.</p></div>{universities.length?<div className="grid3">{universities.slice(0,6).map(item=><UniversityCard key={item.name} {...item}/>)}</div>:<EntityEmpty/>}<div className="author-strip">{authors.slice(0,4).map(item=><AuthorCard key={item.name} {...item}/>)}</div></div></section><section className="section pricing-section"><div className="shell"><div className="section-head"><div><span className="eyebrow">Accès premium</span><h2>Un plan pour chaque ambition.</h2></div><p>Aucun plan gratuit. Chaque abonnement contribue durablement à la conservation et à la diffusion du savoir.</p></div><div className="grid4">{PLAN_CATALOG.map(plan=><PricingCard key={plan.slug} plan={plan} featured={plan.slug==="student-premium"}/>)}</div></div></section><section className="section"><div className="shell"><article className="manifesto glass"><Quote/><span className="eyebrow">Notre engagement</span><blockquote>Rendre le patrimoine scientifique africain découvrable, vérifiable et durable — sans compromettre sa souveraineté.</blockquote><p>Les témoignages publics ne seront affichés qu’après accord et vérification des institutions concernées.</p></article></div></section><section className="section cta-section"><div className="shell cta-inner"><div><span className="eyebrow">Bibliothèque Centrale Universelle</span><h2>Votre recherche mérite<br/>une portée continentale.</h2></div><div><p>Rejoignez l’infrastructure académique pensée pour les étudiants, chercheurs et universités d’Afrique.</p><div className="hero-actions"><Link className="button" href="/pricing">Commencer maintenant<ArrowRight size={17}/></Link><a className="button secondary" href="mailto:institutions@bicuni.online">Parler à notre équipe</a></div></div></div></section></main>}
function aggregate(values:string[]){return [...values.reduce((map,value)=>map.set(value,(map.get(value)??0)+1),new Map<string,number>())].map(([name,count])=>({name,count})).sort((a,b)=>b.count-a.count)}
