import type{Metadata}from"next";import{notFound}from"next/navigation";import{MetadataEditForm}from"@/components/documents/metadata-edit-form";import{db}from"@/lib/db/client";import{requireUser}from"@/lib/auth/guards";import{canEditDocument}from"@/lib/documents/permissions";
import {VersionUpload} from "@/components/documents/version-upload";
export const metadata:Metadata={title:"Modifier le document"};export default async function EditDocument({params}:{params:Promise<{id:string}>}){const user=await requireUser();const[document,categories]=await Promise.all([db.document.findUnique({where:{id:(await params).id},include:{tags:true}}),db.category.findMany({select:{id:true,name:true},orderBy:{name:"asc"}})]);if(!document||!canEditDocument(user,document))notFound();return <main className="shell"><header className="page-hero"><span className="eyebrow">Brouillon</span><h1>Modifier les métadonnées.</h1></header><MetadataEditForm documentId={document.id} initial={document} categories={categories}/><VersionUpload documentId={document.id}/></main>}
