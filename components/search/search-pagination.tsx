import{ChevronLeft,ChevronRight}from"lucide-react";
export function SearchPagination({page,total,pageSize,onPage}:{page:number;total:number;pageSize:number;onPage:(page:number)=>void}){const pages=Math.ceil(total/pageSize);if(pages<=1)return null;return <nav className="search-pagination" aria-label="Pagination"><button className="button secondary" disabled={page<=1} onClick={()=>onPage(page-1)}><ChevronLeft size={16}/>Précédent</button><span>Page {page} sur {pages}</span><button className="button secondary" disabled={page>=pages} onClick={()=>onPage(page+1)}>Suivant<ChevronRight size={16}/></button></nav>}
