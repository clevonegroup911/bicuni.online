import{Building2,Download,Eye,FileText,UserRound}from"lucide-react";import Link from"next/link";import type{AcademicSearchDocument,DirectorySearchDocument}from"@/lib/search/types";
export function SearchResultCard({result}:{result:AcademicSearchDocument}){return <article className="search-result glass card"><div className="result-icon"><FileText/></div><div><div className="result-meta"><span>{result.type}</span><span>{result.year}</span><span>{result.language.toUpperCase()}</span></div><h2><Link href={`/documents/${result.id}`}>{result.title}</Link></h2><p>{result.abstract}</p><div className="result-byline"><strong>{result.authorName}</strong>{result.universityName&&<span>· {result.universityName}</span>}{result.facultyName&&<span>· {result.facultyName}</span>}</div><div className="result-footer"><span>{result.category}</span>{result.tags.slice(0,3).map(tag=><span key={tag}>{tag}</span>)}<i/><span><Eye size={13}/>{result.viewCount}</span><span><Download size={13}/>{result.downloadCount}</span></div></div></article>}
export function DirectoryResultCard({result}:{result:DirectorySearchDocument}){const Icon=result.kind==="university"||result.kind==="faculty"||result.kind==="department"?Building2:UserRound;return <article className="directory-result glass card"><Icon/><div><small>{result.kind}</small><h3>{result.name}</h3><p>{result.subtitle}{result.country?` · ${result.country}`:""}</p></div></article>}
