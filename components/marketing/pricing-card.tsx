import{Check,Crown}from"lucide-react";import Link from"next/link";import{formatPlanPrice}from"@/lib/subscriptions/catalog";
export function PricingCard({plan,featured=false}:{plan:{slug:string;name:string;priceCents:number;description:string;features:readonly string[]};featured?:boolean}){return <article className={`glass card pricing-card ${featured?"featured":""}`}>{featured&&<span className="pricing-label">Le plus choisi</span>}<Crown size={20}/><h3>{plan.name}</h3><p>{plan.description}</p><div className="price"><strong>{formatPlanPrice(plan.priceCents)}</strong><span>/ mois</span></div><ul>{plan.features.slice(0,4).map(feature=><li key={feature}><Check size={14}/>{feature}</li>)}</ul><Link href="/pricing" className={`button ${featured?"":"secondary"}`}>Découvrir l’offre</Link></article>}
