import{BookOpen,Building2,FileCheck2,Users}from"lucide-react";
export function StatisticsCards({documents,universities,authors}:{documents:number;universities:number;authors:number}){const stats=[[BookOpen,documents,"Publications indexées"],[Building2,universities,"Institutions représentées"],[Users,authors,"Auteurs visibles"],[FileCheck2,"100 %","Dépôts validés"]]as const;return <div className="grid4 stats-grid">{stats.map(([Icon,value,label])=><article className="glass card stat-card" key={label}><Icon size={19}/><strong>{value}</strong><span>{label}</span></article>)}</div>}
