"use client";
import { FormEvent,useState }from"react";import{useRouter}from"next/navigation";
export function CreateAdminForm(){const router=useRouter();const[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget;const response=await fetch("/api/admin/users",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const result=await response.json()as{error?:string};setMessage(response.ok?"Administrateur créé.":result.error??"Création refusée.");if(response.ok){form.reset();router.refresh()}}return <details className="glass card create-admin"><summary>Créer un administrateur</summary><form className="form-grid" onSubmit={submit}><label>Nom<input className="input" name="name" required/></label><label>Email<input className="input" name="email" type="email" required/></label><label>Rôle<select className="input" name="role"><option>ADMIN</option><option>MODERATOR</option><option>INSTITUTION_ADMIN</option><option>SUPER_ADMIN</option></select></label><label>Mot de passe temporaire<input className="input" name="password" type="password" minLength={12} required/></label><button className="button">Créer</button>{message&&<p role="status">{message}</p>}</form></details>}
