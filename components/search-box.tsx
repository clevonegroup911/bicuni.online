"use client";
import{Search,SlidersHorizontal}from"lucide-react";import{useRouter}from"next/navigation";import{FormEvent,useState}from"react";
export function SearchBox({compact=false,defaultValue="",destination}:{compact?:boolean;defaultValue?:string;destination?:string}){const[query,setQuery]=useState(defaultValue);const router=useRouter();function submit(event:FormEvent){event.preventDefault();const target=destination??(compact?"/library":"/search");router.push(`${target}?q=${encodeURIComponent(query.trim())}`)}return <form onSubmit={submit} aria-label="Recherche académique" className={`search-bar ${compact?"compact":""}`}><Search size={20}/><input value={query} onChange={event=>setQuery(event.target.value)} aria-label="Termes de recherche" placeholder="Titre, auteur, université, année, domaine…"/>{!compact&&<button type="button" className="icon-button" aria-label="Afficher les filtres"><SlidersHorizontal size={17}/></button>}<button className="button" type="submit">Rechercher</button></form>}
export const SearchBar=SearchBox;
